# Dynatrace credentials — populated by setup script from codespace secrets.
apiVersion: v1
kind: Secret
metadata:
  name: dt-credentials
  namespace: daystrom-ai
type: Opaque
stringData:
  DT_ENDPOINT: "PLACEHOLDER"
  DT_API_TOKEN: "PLACEHOLDER"
